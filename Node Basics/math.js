const sum = (a,b) => {
    return a+b;
}

const mul = (a,b) => {
    return a*b;
}


const PI = 3.14;
const GRA = 9.8;


let obj = {
    sum : sum,
    mul : mul,
    pi : PI,
    gra : GRA,
};

module.exports = obj;
